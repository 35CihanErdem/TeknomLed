/**
 * Storefront filter option constants only.
 * Product catalogue data comes from the backend (Phase 8).
 * Seeded DB products are DEVELOPMENT/TEST data — not official customer catalog.
 */

export const FILTER_KELVINS = [2700, 3000, 4000, 6500] as const;
export const FILTER_IP_CLASSES = ['IP44', 'IP54', 'IP65', 'IP67'] as const;
